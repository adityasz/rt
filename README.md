# Raytracer

My implementation of [Ray Tracing in One Weekend](https://raytracing.github.io/books/RayTracingInOneWeekend.html)
with (naive) multithreaded rendering, and PNG and BMP export for faster
rendering and smaller output image.

The raytracer is a library with everything in the `rt` namespace.
